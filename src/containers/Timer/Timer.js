import React from 'react';
import Header from '../../components/header/header';

const timer = props => {
    return (
        <div>
            <Header>
                <h3>Timer</h3>
            </Header>
        </div>
    );
};

export default timer;